# Provenance

The following files are authoritative copies fetched from the canonical repo
`skytechnext/full1` (branch `claude/casa-escondida-odoo-erp-o9r79d`), via its public
`raw.githubusercontent.com` endpoint, and are the source of truth for the engine's
reference example project:

- `ai-blackbox.html` — canonical conceptual model (AGENTS / LOOPS / STAGES / STACK / OPS / COUNT_KEYS)
- `agents/orchestration.md` — agent roster + orchestration loop
- `BUILD-LOG.md` — what was actually built over the live API vs. browser-only steps
- `projects/casa-escondida/odoo-build-spec/{modules,demo-data,automations,data-model}.json`, `mcp-runbook.md`, `hotel_setup.py`
- `reference/connection-strategy.md` (= full1 odoo-build-spec/connection.md)

Canonical site: https://skytechnext.github.io/full1/ai-blackbox.html
